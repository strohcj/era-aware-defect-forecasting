# Normalize text files to LF in the repo
* text=auto eol=lf

# Keep these as binary (no line-ending changes)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.zip binary
*.npz binary
*.parquet binary

# If you need Windows endings for scripts, opt in explicitly:
*.bat text eol=crlf
*.ps1 text eol=crlf

